import React from "react";

export default function AboutPage() {
  return (
    <div>
      <h1>About</h1>
      <p>
        This is a simple web application that allows users to search for items
        by category, company, or brand. The data is fetched from a local server
        and displayed in a list. The user can filter the list by typing in the
        search bar.
      </p>
    </div>
  );
}
